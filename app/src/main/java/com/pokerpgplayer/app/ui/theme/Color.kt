package com.pokerpgplayer.app.ui.theme

import androidx.compose.ui.graphics.Color

// Echoes the PokeRPG Player OS's default dark theme + teal accent, so the
// Android app and the project's own desktop tooling feel like the same
// product family. Not a hard requirement — easy to restyle later.

val TealAccent = Color(0xFF2DD4BF)
val TealAccentStrong = Color(0xFF19B3A0)

val DarkBackground = Color(0xFF0A0C11)
val DarkSurface = Color(0xFF11141B)
val DarkSurfaceVariant = Color(0xFF1E232E)
val DarkOnBackground = Color(0xFFE8EAEE)
val DarkOnSurfaceVariant = Color(0xFF9AA3B5)
val DarkOutline = Color(0xFF262C38)

val LightBackground = Color(0xFFF3F4F7)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFEDEFF3)
val LightOnBackground = Color(0xFF14161B)
val LightOnSurfaceVariant = Color(0xFF5B6472)
val LightOutline = Color(0xFFE1E4EA)

val DangerRed = Color(0xFFFB7185)
