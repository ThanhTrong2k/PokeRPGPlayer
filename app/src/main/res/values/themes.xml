<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme: Compose draws everything, so this just
         needs to be a no-ActionBar theme. -->
    <style name="Theme.PokeRPGPlayer" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen theme (androidx.core.splashscreen).
         windowSplashScreenAnimatedIcon points at a simple vector drawable
         placeholder (drawable/ic_splash_logo.xml) — original artwork only,
         no Pokémon trademarked imagery. -->
    <style name="Theme.PokeRPGPlayer.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background_light</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background_light</item>
        <item name="postSplashScreenTheme">@style/Theme.PokeRPGPlayer</item>
    </style>


    <style name="Theme.PokeRPGPlayer.Runtime.NoActionBar" parent="android:Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
